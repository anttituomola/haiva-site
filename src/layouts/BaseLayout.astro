---
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

interface Props {
  title?: string;
  description?: string;
}

const { title = 'Häivä', description = 'Almost like touching you' } = Astro.props;
---

<!doctype html>
<html lang="fi">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" href="/favicon.ico" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    
    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content="https://haiva.art/" />
    <meta property="og:title" content="Häivä" />
    <meta property="og:description" content="Almost like touching you" />
    <meta property="og:image" content="https://haiva.art/haiva-logo-v5.png" />
    
    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image" />
    <meta property="twitter:url" content="https://haiva.art/" />
    <meta property="twitter:title" content="Häivä" />
    <meta property="twitter:description" content="Almost like touching you" />
    <meta property="twitter:image" content="https://haiva.art/haiva-logo-v5.png" />
  </head>
  <body>
    <Header />
    <main class="main">
      <slot />
    </main>
    <Footer />
  </body>
</html>

<style is:global>
  :root {
    --color-bg: #050508;
    --color-text: rgba(255, 255, 255, 0.9);
    --color-text-muted: rgba(255, 255, 255, 0.55);
  }
  *,
  *::before,
  *::after {
    box-sizing: border-box;
  }
  html {
    font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
    color: var(--color-text);
    background: var(--color-bg);
  }
  body {
    margin: 0;
    min-height: 100vh;
  }
  .main {
    position: relative;
    z-index: 5;
    min-height: calc(100vh - 120px);
  }
</style>
